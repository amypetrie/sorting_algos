const assert = require('chai').assert
const merge = require('../merge')

describe('merge functionality', function() {
  context('merges two sorted arrays into one sorted array', function(){
    it('can merge two arrays of length two', function(){
      first_array= [6,7]
      second_array = [3,5]

      //result = merge(first_array, second_array)

      //assert.deepEqual(result, [3,5,6,7])
    })
  })
})
