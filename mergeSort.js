// a mergeSort function which splits an unsorted array into pieces so that they can be sorted.
// mergeSort recursively calls mergeSort on itself, ending up with two sorted arrays
// calls the merge function on the two sorted arrays

function mergeSort(unsorted_collection){

}

module.exports = mergeSort
